# Backend configuration (no secrets are committed to the repository)
MAX_FILE_SIZE_MB=15
ALLOWED_ORIGINS=http://localhost:3000
FINHEALTH_DB=./app.db
# Optional LLM key for narrative recommendations. The app works WITHOUT it:
# rule-based recommendations are always generated locally.
# ANTHROPIC_API_KEY=
